const Title = ({ title }) => {
  return (
    <div
      style={{
        textAlign: "center",
        color: "royalblue",
        padding: "20px",
        fontSize: "20px",
      }}
    >
      <h1> {title}</h1>
    </div>
  );
};

export default Title;
